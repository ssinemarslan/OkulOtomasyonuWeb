@model IEnumerable<Okul.Models.DerslerModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "EY")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DersAdi)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Kredi)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.DersAdi)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Kredi)
    </td>
    <td>
        @Html.ActionLink("Edit", "EY", new { id = item.DersNo }) |

        @Html.ActionLink("Delete", "Delete", new { id = item.DersNo })
    </td>
</tr>
}

</table>
